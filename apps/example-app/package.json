{
  "name": "@apps/example-app",
  "description": "Example SvelteKit App",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "vitest --run",
    "lint": "eslint .",
    "clean": "del .svelte-kit .turbo"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "2.1.1",
    "@sveltejs/kit": "1.27.3",
    "@toolchain/eslint-config": "workspace:*",
    "svelte": "4.2.2",
    "svelte-check": "3.5.2",
    "tslib": "2.6.2",
    "vite": "4.5.0"
  }
}
